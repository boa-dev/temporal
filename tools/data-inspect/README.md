# Data inspect tool

The primary purpose of `data-inspect` is to provide some visibility
to `timezone_provider`'s data.

At it's core, it works very similiarly to `tzif-inspect` so that
comparisons are relatively easy as well.

To use:

```bash
cargo run -p tzif-inspect -- Europe/London
```

